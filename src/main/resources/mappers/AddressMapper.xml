<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ru.ovechnikov.emplist.mapper.AddressMapper">

    <select id="getRegionList" resultType="String">
        SELECT DISTINCT(a.region) FROM addresses a;
    </select>

    <select id="getDistrictList" resultType="String">
        SELECT DISTINCT(a.district) FROM addresses a;
    </select>

    <update id="updateAddress">
        UPDATE addresses SET
            address = #{address},
            region = #{region},
            district = #{district}
        WHERE employee_id = #{id};
    </update>

    <insert id="saveNewAddress">
        INSERT INTO addresses (employee_id, address, region, district)
        VALUES (#{id}, #{address}, #{region}, #{district});
    </insert>

</mapper>